/**
 * useEncryption — deprecated. All encryption now flows through MessageService + KeyManager.
 * Kept as an empty stub to avoid breaking any future consumer that may reference it.
 */
export function useEncryption() {
  return {};
}
